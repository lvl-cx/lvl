-- when player spawns if first spawn
-- send this ARMA:setDailyRewardInfo with parameters (streak, )

-- allow player to trigger ARMA:claimNextLoginReward and it will 
-- give them the next reward they have due